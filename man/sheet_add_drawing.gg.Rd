% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_xlsx.R
\name{sheet_add_drawing.gg}
\alias{sheet_add_drawing.gg}
\title{Add a ggplot to an Excel sheet}
\usage{
\method{sheet_add_drawing}{gg}(
  x,
  value,
  sheet,
  left = 1,
  top = 1,
  width = 6,
  height = 4,
  res = 300,
  alt_text = "",
  scale = 1,
  ...
)
}
\arguments{
\item{x}{rxlsx object created by \code{\link[=read_xlsx]{read_xlsx()}}}

\item{value}{a ggplot object (class \code{gg})}

\item{sheet}{sheet name (must already exist)}

\item{left, top}{top-left anchor of the image, in inches}

\item{width, height}{size of the image, in inches}

\item{res}{resolution of the png image, in ppi (default 300)}

\item{alt_text}{alt text for screen readers. If \code{""} or \code{NULL},
\code{ggplot2::get_alt_text()} is consulted.}

\item{scale}{multiplicative scaling factor, same as \code{ggplot2::ggsave(scale=)}}

\item{...}{passed to \code{\link[ragg:agg_png]{ragg::agg_png()}}}
}
\value{
the rxlsx object (invisibly)
}
\description{
Render a ggplot object to PNG and embed it into a sheet
of an xlsx workbook via \code{\link[=sheet_add_drawing.external_img]{sheet_add_drawing.external_img()}}. For an
editable vector-graphics version, use \code{rvg::dml(ggobj = ...)} +
\code{rvg::sheet_add_drawing.dml()} instead.
}
\seealso{
\code{\link[=sheet_add_drawing]{sheet_add_drawing()}}, \code{\link[=sheet_add_drawing.external_img]{sheet_add_drawing.external_img()}}
}
