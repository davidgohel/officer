% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_xlsx.R
\name{sheet_add_drawing.external_img}
\alias{sheet_add_drawing.external_img}
\title{Add an image to an Excel sheet}
\usage{
\method{sheet_add_drawing}{external_img}(
  x,
  value,
  sheet,
  left = 1,
  top = 1,
  width = NULL,
  height = NULL,
  ...
)
}
\arguments{
\item{x}{rxlsx object (created by \code{\link[=read_xlsx]{read_xlsx()}})}

\item{value}{an \code{\link[=external_img]{external_img()}} object}

\item{sheet}{sheet name (must already exist)}

\item{left, top}{top-left anchor of the image, in inches. Defaults
to \verb{(1, 1)}.}

\item{width, height}{size of the image, in inches. When \code{NULL}
(default) the dimensions stored on \code{value} (from \code{external_img()})
are used.}

\item{...}{unused}
}
\value{
the rxlsx object (invisibly)
}
\description{
Add an image file (PNG, JPEG, GIF, ...) to a sheet in
an xlsx workbook created with \code{\link[=read_xlsx]{read_xlsx()}}. The image is copied
into \verb{xl/media/} and placed on the sheet via an absolute anchor
(inch-based position and size).
}
\examples{
img <- system.file("extdata", "example.png", package = "officer")
if (nzchar(img) && file.exists(img)) {
  x <- read_xlsx()
  x <- add_sheet(x, label = "pics")
  x <- sheet_add_drawing(
    x, sheet = "pics",
    value = external_img(img, width = 2, height = 2)
  )
  print(x, target = tempfile(fileext = ".xlsx"))
}
}
\seealso{
\code{\link[=sheet_add_drawing]{sheet_add_drawing()}}, \code{\link[=external_img]{external_img()}}
}
