# compress a folder

compress a folder to a target file. The function returns the complete
path to target file.

## Usage

``` r
pack_folder(folder, target)
```

## Arguments

- folder:

  folder to compress

- target:

  path of the archive to create
